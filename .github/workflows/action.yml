# action.yml
name: 'Trusty'
on: push
jobs: 
  test-ctf-container:
    runs-on: ubuntu-latest
    steps:
      - name: Build the container
        run: pwd ; ls ; ls ../ ; docker build ../. --file Dockerfile -t ctf_image
